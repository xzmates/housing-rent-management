# 实施任务清单

- [x] 1. 优化微信提醒摘要生成
  - 将分组计数文案从“类”改为“项”。
  - 汇总未结清账单总数和逾期账单数。
  - 生成 `全部逾期`、`部分逾期`、`暂无逾期` 三态文案。
  - 按“逾期优先、金额降序、房间稳定排序”生成提醒顺序。
  - 生成金额优先的前两项备注；超过两项时合并为“另N项+其余金额”。
  - 自动提取房间编号、压缩租客姓名并确保备注不超过20字符。
  - _需求：1、2、3_

- [x] 2. 完善单元测试和回归测试
  - 更新现有模板字段断言。
  - 覆盖截图示例、三种逾期状态、两项/多项压缩、长姓名及字段长度。
  - 运行提醒专项测试和项目完整测试集。
  - _需求：1、2、3_

- [ ] 3. 编译并上传小程序体验版
  - 核对 `project.config.json` 的 AppID、源码目录和云函数目录。
  - 使用微信开发者工具编译，确认无 JS、WXML、WXSS 错误。
  - 上传新的小程序体验版本，并记录版本号与上传结果。
  - 当前结果：版本 `1.0.20260811` 已上传为开发版本；等待管理员在微信公众平台执行“选为体验版”。
  - _需求：4_

- [x] 4. 部署云函数并切换体验版跳转
  - 查询 `rentalDomain` 当前配置并保留全部环境变量。
  - 部署通过测试的云函数代码。
  - 将 `WECHAT_MINIPROGRAM_STATE` 设置为 `trial`，不输出任何密钥。
  - 验证定时触发器和模板字段配置仍然存在。
  - _需求：1、2、3、4_

- [ ] 5. 手动触发并完成真机验收
  - 手动触发一次当前待收提醒。
  - 核对微信卡片金额、类型、项数、逾期笔数、状态、最早截止日期和备注。
  - 点击消息进入体验版首页，确认不再显示“小程序尚未发布”。
  - 核对同日幂等、发送状态及额度仅在成功后扣减。
  - 当前结果：已归档原成功投递、重置当日幂等记录并重新发送；返回 `sent: 1`、`failed: 0`、`duplicate: 0`。消息内容正确，但微信提示“体验版不存在”；等待将上传版本选为体验版后再次点击验收。
  - _需求：1、2、3、4_
